export function FilmPerforations({ className = "" }: { className?: string }) {
  return (
    <div
      className={`h-3 w-full film-perforations opacity-40 ${className}`}
      aria-hidden
    />
  );
}
